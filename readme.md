# Olimex CCGM1A1 PSRAM 

This is an attempt at driving the PSRAM at their maximum speed (100 MHz).

## Tests

Some tests are made using cocotb, which comes with the oss-cad-suite. Other tests are running on Veryl's native simulator and require no other dependencies.